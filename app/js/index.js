/* styling */
require('styles/main.scss');
/* js */
import $ from 'jquery';
import { log, logTitle } from 'logger';
/* your imports */

logTitle('Title')


// javascript objects

/* let person = {
    name: 'Bobo',
    age: 27,
    hasPassport: true,
    address: {
        house_number: 1,
        streetname: 'Redeemer Avenue',
        country: 'Nigeria'
    }
};
log(person);
log(JSON.stringify(person));
log(person.name);
log(person.age);
log(person.hasPassport);
log(Object.values(person));
log(JSON.stringify(person.address)); 
*/

// javascript arrays